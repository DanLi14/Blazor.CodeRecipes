@* creating components with customisable content using RenderFragment which represents a segment of UI content
i.e. allows components to accept arbitrary HTML as a parameter
*@

@page "/ch01r07"
@using BlazorCookbook.App.Client.Data

<Ticket Id="@Samples.Adult.Id">@Samples.Adult.Tariff (@Samples.Adult.Price)</Ticket> @* // render both tariff and price *@ 
<Ticket Id="@Samples.FreeAdmission.Id">
    <div class="free-ticket">@Samples.FreeAdmission.Tariff</div> @* // render only the tariff name *@ 
</Ticket>

@code {

}
