@typeparam T

<table class="grid">
    <thead>
        @Header
    </thead>
    <tbody>
        @foreach (var item in Data)
            @Row(item)
    </tbody>
</table>

@code {
    // accepts any data type and T is the parameter type placeholder which allows us to operate on T inside the component 
    [Parameter, EditorRequired]
    public IList<T> Data { get; set; }

    [Parameter, EditorRequired]
    public RenderFragment Header { get; set; }

    [Parameter, EditorRequired]
    public RenderFragment<T> Row { get; set; }
}
