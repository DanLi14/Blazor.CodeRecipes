@* since IntroductionForm exposes Name and Skill parameters with EventCallback, we can dynamically bind them using @bind-Name and @bind-Skill
    the bind-value pattern encapsulates binding logic & validation within a component. It only requires the parent component to provide backing variables*@

@page "/ch02r05"
@rendermode InteractiveWebAssembly
@using BlazorCookbook.App.Client.Chapter2.Data

<IntroductionForm @bind-Name="Name" @bind-Skill="Skill" /> @*  example of bind-value pattern  *@

<hr />

@if (!IsGreetingReady)
{
    return;
}

<h5>Welcome @Name on level @Skill.Title!</h5>


@code {
    protected string Name { get; set; }

    protected SkillLevel Skill { get; set; }

    private bool IsGreetingReady => !string.IsNullOrWhiteSpace(Name) && Skill is not null;

}

